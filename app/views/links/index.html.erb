<%= paginate @links %>
<h1>Listing links</h1>


<% @links.each do |link| %>
  <ol>
    <li><%= link_to link.title, link.url, :target => "_blank" %>
		<%= link_to "Comments", "/links/#{link.id}"%></li>
  </ol>
<% end %>


<br />


